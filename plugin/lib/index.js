// Host face of the quick image editor package: all editing runs in the browser.
// This host plugin exists so the Loader row activates and the client-modules
// scan can discover the package's dsh.client declaration.
export const name = "dsh-quick-image-editor"
export function apply(ctx) {}
